# Copie ce fichier en ".env" pour tester en local (ce fichier n'est jamais envoyé sur GitHub).
# Sur Vercel, cette variable se configure dans Settings > Environment Variables — jamais ici.
SNCB_API_KEY=colle_ta_cle_ici
